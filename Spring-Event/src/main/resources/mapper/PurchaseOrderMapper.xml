<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hello.springevent.mapper.PurchaseOrderMapper">

    <insert id="insertOrder" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sq_purchase_order (user_id, product_id, coin_amount, status)
        VALUES (#{userId}, #{productId}, #{coinAmount}, #{status})
    </insert>
</mapper>

