<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hello.springevent.mapper.UserAssetMapper">

    <insert id="ensureUserAsset">
        INSERT INTO sq_user_asset (user_id, coin, point, experience)
        VALUES (#{userId}, 100, 0, 0)
        ON DUPLICATE KEY UPDATE user_id = user_id
    </insert>

    <update id="deductCoin">
        UPDATE sq_user_asset
        SET coin = coin - #{coin},
            update_time = NOW()
        WHERE user_id = #{userId}
          AND coin &gt;= #{coin}
    </update>

    <update id="addReward">
        UPDATE sq_user_asset
        SET coin = coin + #{coinDelta},
            point = point + #{pointDelta},
            experience = experience + #{experienceDelta},
            update_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <select id="selectCoin" resultType="java.lang.Integer">
        SELECT coin
        FROM sq_user_asset
        WHERE user_id = #{userId}
    </select>
</mapper>

